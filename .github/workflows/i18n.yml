name: i18n — Auto-translate on push

on:
  # Run on every push to main — Lingo.dev CI only processes new/changed strings
  push:
    branches: [main]

  workflow_dispatch: {}

jobs:
  translate:
    name: Translate missing locale strings
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Run Lingo.dev CI translation pipeline
        run: npx lingo.dev@latest ci --no-interactive
        env:
          LINGO_API_KEY: ${{ secrets.LINGO_API_KEY }}
          LINGODOTDEV_API_KEY: ${{ secrets.LINGO_API_KEY }}

      - name: Commit translated locales
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "i18n: auto-translate locale files [skip ci]"
          file_pattern: "locales/*.json"
